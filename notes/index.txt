- Yii2
- docker compose build
docker compose up -d

DBeaver: Mysql:
Connected by Host
    Server host: localhost Port: 3306
    Database: yii2db
    Username: yii2user
    Pass: yii2pass

- docker ps
- docker exec -it yii2-learning bash
- cd yii2-app
- php yii migrate/create create_table_1_table
- php yii migrate

API: Get http://localhost:8000/index.php?r=user/index => http://localhost:8000/user/index (UserController, actionIndex)
thì phải dùng Pretty URL
https://www.yiiframework.com/doc/guide/2.0/en/runtime-routing

Truy cập http://localhost:8080/debug để xem tất cả câu query
Mỗi lần chạy 1 API , sẽ sinh ra 1 record ở trang này.
Ở file runtime/debug/   *.data

Dump Database file Dbeaver:
    - Dump db mysqldump (Nhiều khi DBeaver không hỗ trợ đâu)
    - docker exec -it yii2-learning bash
    - cd yii2-app/
    - mysqldump -u root -proot yii2db > database_dump_12_10_25.sql

    docker cp "/home/duc-ubuntu-laptop/Documents/projects/Personal/Yii2/database_backup/database_dump_01_10_25.sql" yii2-learning:/tmp/
    mysql -u root -p'rootpassword' yii2db < /tmp/database_dump_01_10_25.sql

    Nếu gặp lỗi không thấy db yii2db
    => docker exec -it yii2-learning bash
    => cd yii2-app/
    => mysql -u root -p123456 -e "CREATE DATABASE IF NOT EXISTS yii2db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"